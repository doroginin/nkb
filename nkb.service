[Unit]
Description=Nuclear Keyboard

[Service]
Type=simple

# Wait user login
ExecStartPre=/bin/bash -c 'while [[ ! $(who) ]]; do sleep 1; done'
# add -v option for verbose mode
ExecStart=/usr/local/bin/nkb -d 4

Restart=always

[Install]
WantedBy=multi-user.target
